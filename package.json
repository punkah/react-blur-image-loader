{
  "name": "react-blur-image-loader-ts",
  "version": "1.0.0",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "repository": "https://github.com/punkah/react-blur-image-loader-ts.git",
  "files": [
    "dist",
    "README.md"
  ],
  "author": "punkah",
  "license": "MIT",
  "dependencies": {
    "react": "^17.0.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.16.0",
    "@babel/core": "^7.5.4",
    "@babel/preset-env": "^7.5.4",
    "@babel/preset-react": "^7.0.0"
  },
  "scripts": {
    "build": "rm -rf dist && NODE_ENV=production babel src --out-dir dist --copy-files"
  }
}
